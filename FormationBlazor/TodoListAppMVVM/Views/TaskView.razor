@page "/"
<h1>To-Do List</h1>

@* <input type="text" placeholder="Ajouter une nouvelle tâche" @bind="ViewModel.NewTaskTitle" /> *@
@* <button @onclick="ViewModel.AddTask">Ajouter</button> *@
@* *@
@* <ul> *@
@*     @foreach (var task in ViewModel.Tasks) *@
@*     { *@
@*         <li> *@
@*             <input type="checkbox" @bind="task.IsCompleted" @onclick="() => ViewModel.ToggleTaskCompletion(task)" /> *@
@*             <span style="@((task.IsCompleted ? "text-decoration: line-through;" : ""))">@task.Title</span> *@
@*             <button @onclick="() => ViewModel.RemoveTask(task)">Supprimer</button> *@
@*         </li> *@
@*     } *@
@* </ul> *@

<TaskFormComponent ></TaskFormComponent>
<TaskListComponent ></TaskListComponent>

@code {
    
}