<li>
            <input type="checkbox" @bind="Task.IsCompleted" @onclick="ToggleCompletion" />
            <span style="@((Task.IsCompleted ? "text-decoration: line-through;" : ""))">@Task.Title</span>
            <button @onclick="RemoveTask">Supprimer</button>
        </li>

@code {
    
}